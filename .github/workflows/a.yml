on:
  workflow_dispatch:
  schedule:
    - cron: '45 12,15,18,21 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay/it1h-1@cron
        with:
          token: ${{ secrets.token }}
